#include "main.h"
/**
 * rev_string - reverses a string
 * @s: function parameter
 * Return: 0
 */
void rev_string(char *s)
{
	int i;

	for (i = 0 ; str[i] != '\0' ; i++)
	{
		_putchar(str[i])
			i++;
	}
	_putchar('\n');
}
